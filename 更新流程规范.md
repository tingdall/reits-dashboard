# 机构间REITs数据看板 · 更新流程规范（增量更新策略）

> 本文档定义**每次数据更新**必须遵守的增量处理规范，与 `reits_update.py` 的实现一一对应。
> 所有后续更新（无论由人还是 AI 执行）必须遵循本规范，禁止"全量重算一切"。

## 核心原则

**只处理变化的部分，复用未变的数据。** 更新流程 = 抓取增量 + 继承存量 + 仅在必要时重算。

---

## 1. 分类字段（项目类型 / 二级分类）的增量规则

### 1.1 默认行为：继承，不重算

每次更新时，**项目类型（ptype）、二级分类（psub）、待核标记（pflag）、判断依据（pnote）** 四个字段：

- 按项目 `id` 与**旧快照**（`reits_snapshot.js`）匹配 → **原样沿用**，不做任何重新计算
- 旧快照无记录但 **`cls_seed.json`**（分类种子）命中 → 采用种子分类（用于首次建立基线或补漏）
- 都不命中（**全新项目**）→ 分类留空，`pflag="待分类"`、`pnote="新增项目，请人工确认项目类型与二级分类"`，由前端显示「待分类」标记、人工在界面上补充

**为什么**：分类是基于名称/底层资产的人工+检索综合判断，每次全量重算既浪费（网络检索成本高）又会覆盖人工在界面上的手动修正（✎）。

### 1.2 触发重新分类的条件（仅两类）

| 触发条件 | 处理 |
|---|---|
| **新增项目**（交易所接口出现新 id） | 仅该新项目标记「待分类」，其余 105 条沿用不动 |
| **分类规则版本变更** | `reits_update.py` 中 `CLS_VERSION`（当前 `2026-08-06-v1`）递增 → 脚本打印"触发全量重新分类（请人工核对）"，所有项目分类清空为待分类，由人工按新规则重新分类 |

**明确禁止**：无以上两个条件时，不允许对已有项目的分类做任何修改或重算。

### 1.3 版本号管理

- `CLS_VERSION` 位于 `reits_update.py` 常量区，格式 `YYYY-MM-DD-vN`
- **何时递增**：PTYPE_SUBS 分类体系变化（增删二级分类）、判定规则调整、底层资产分类口径变化
- 快照输出带 `var __REITS_CLS_VERSION__ = "..."`，供脚本下次运行比对

---

## 2. 其他字段/模块的增量原则

| 模块 | 增量处理方式 |
|---|---|
| 基础字段（status/amt/up/acc/origin/writer/mgr） | 随抓取自然更新——只写入**当前抓取到的值**，未变化项目无需特殊处理（覆盖为同值，等效无变化） |
| 编号（num） | 每次更新后按默认排序**重排全局编号 1..N**（这是有意的确定性重算，因为排序/新增会改变相对位置） |
| 手动分类覆盖（前端 localStorage `reits_dash_cls_v1`） | 完全在浏览器端，脚本不触碰；快照分类仅作为默认值，前端手动覆盖始终优先 |
| 标记/自定义标签（前端 localStorage） | 同上，浏览器端独立，与数据更新无耦合 |
| 图表统计 | 前端根据当前筛选结果**实时计算**，无持久化，天然增量（不缓存） |
| 内嵌数据（HTML 内 RAW_DATA） | 由 `build_reits.py` 从最新快照生成时整体替换——这是**构建时**的动作，不是数据计算，允许整体更新 |

---

## 3. 标准更新流程（必须按此顺序）

```
1. python reits_update.py
   ├─ 抓取两所最新数据（仅本次变化自然体现）
   ├─ 增量继承分类字段（旧快照/cls_seed 命中沿用，新项目标待分类）
   ├─ 排序 + 全局编号
   └─ 输出 reits_snapshot.js（含 __REITS_CLS_VERSION__）

2. 人工处理新项目（若有"待分类"）
   ├─ 打开看板 → 在界面上点击单元格手动分类（自动存 localStorage + 快照下次沿用）
   └─ 或将分类补进 cls_seed.json 作为永久基线

3. python build_reits.py        # 用新快照重建正式 HTML（内嵌数据更新）

4. 回归验证（若改动过模板/脚本）
   ├─ node --check 语法检查
   └─ Chrome 无头回归（参考 tests/ 下回归脚本）
```

---

## 4. 禁止事项（红线）

- ❌ 每次更新都重新全量计算所有项目的 ptype/psub（会覆盖人工修正、浪费检索）
- ❌ 在无版本变更时擅自修改已有项目的分类值
- ❌ 直接手改正式 HTML（必须改模板 → build_reits.py 生成）
- ❌ 删除/重命名 `cls_seed.json`（它是分类基线，删除会导致下次更新大量项目变待分类）

---

## 5. 文件职责（谁负责什么）

| 文件 | 职责 |
|---|---|
| `reits_update.py` | 抓取 + 增量合并 + 编号 + 快照输出（含 CLS_VERSION） |
| `cls_seed.json` | 分类基线种子（id → 分类），首次建立/补漏用 |
| `reits_snapshot.js` | 数据快照（含分类字段 + 版本号），页面加载源 |
| `build_reits.py` | 模板+快照 → 正式 HTML（构建动作） |
| 前端 HTML | 展示 + 手动分类覆盖（localStorage 优先于快照） |
